gradio>=6.0.0
httpx>=0.27.0
msgpack>=1.0.8
pandas>=2.0.0
python-dotenv>=1.0.0

# Translation — Groq (default), Gemini, or Ollama (fallback)
groq>=0.11.0
google-genai>=1.0.0

# Music / vocal separation (Demucs)
# Install torch first for your platform: https://pytorch.org/get-started/locally/
# Then:
demucs>=4.0.0

# ASR — WhisperX (default backend; uses faster-whisper + pyannote for diarization)
# Install torch first for your platform: https://pytorch.org/get-started/locally/
# Then:
whisperx>=3.1.0

# Phase B — overlapping-speaker handling
# pyannote and torchaudio are already pulled in as whisperx transitive deps,
# but pin them explicitly for Phase B (SepFormer separation):
pyannote.audio>=4.0.0
speechbrain>=1.0.0
# torchaudio 2.8.x: must stay on the 2.8 line.
#   - torchaudio 2.9+ removes AudioMetaData (still used by pyannote.audio 3.x),
#     and violates whisperx 3.8.x's own torchaudio~=2.8.0 constraint.
#   - torchaudio 2.8.0 is available on the cu128 index, so Blackwell/sm_120
#     GPU support is preserved when setup.bat/setup.sh installs from that index.
torchaudio~=2.8.0
